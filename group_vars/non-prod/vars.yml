---
# Add environment specific settings here

# aws-sts settings
# At a minimum you must specify the ARN of the IAM role
# that should be assumed to run your provisinoing tasks
# e.g. sts_role_arn: "arn:aws:iam::012345678901:role/admin"
sts_role_arn: "ARN of your IAM role"

# environment specific settings are always prefixed with config_
config_my_stack_input: "{{ 10000 | random }}"